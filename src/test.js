var format = function (string, values) {
  // write content of this function
};

var sentence = "The {adjectives} {noun} {verb} over the lazy dog";

// this is the function call that takes two arguments
format(sentence, { adjectives: "quick brown", noun: "fox", verb: "jumped" });

// with the example given, format function should return: the quick brown fox jumped over the lazy dog
